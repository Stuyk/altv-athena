import { RGB } from '../../shared/interfaces/rgb';

export interface iPaintshopSync {
    color: number | RGB;
    color2: number | RGB;
    pearl: number;
    finish1: number;
    finish2: number;
}
