/**
 * An enum that defines storage checks / behavior propagation
 * OPEN
 * @export
 * @enum {number}
 */
export const enum STORAGE_RULES {
    OPEN = 'storage-open',
}

/**
 *
 *
 * @export
 * @enum {number}
 */
export const enum STORAGE_MOVE_RULES {
    FROM_STORAGE = 'from-storage',
    TO_STORAGE = 'to-storage',
}
