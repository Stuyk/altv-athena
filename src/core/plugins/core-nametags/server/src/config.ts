import { INametagConfig } from '../../shared/interfaces';

// Hover over these in VSCode to see what they do.
export default {
    MAX_DRAW_DISTANCE: 50,
    SHOULD_SCREEN_SHAKE: true,
    SHOW_NAME: true,
    SHOW_ID: true,
    SHOW_NAME_INSTANTLY: false,
    SHOW_NAMETAGS_WITH_KEY: false,
    NAME_TAGS_KEY: 18, // Left Alt
} as INametagConfig;
