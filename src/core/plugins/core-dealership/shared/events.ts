const PAGE_NAME = 'Dealership';

export const DEALERSHIP_EVENTS = {
    LOAD: `${PAGE_NAME}:Load`,
    PURCHASE: `${PAGE_NAME}:Purchase`,
    EXIT: `${PAGE_NAME}:Exit`,
};

export const DEALERSHIP_WEBVIEW_EVENTS = {
    PAGE_NAME: PAGE_NAME,
    READY: `${PAGE_NAME}:Ready`,
    PURCHASE: `${PAGE_NAME}:Purchase`,
    SET_VEHICLES: `${PAGE_NAME}:SetVehicles`,
    EXIT: `${PAGE_NAME}:Exit`,
    PREVIEW: `${PAGE_NAME}:Preview`,
    CAMERA: `${PAGE_NAME}:Camera`,
    SET_BANK: `${PAGE_NAME}:SetBank`,
};
